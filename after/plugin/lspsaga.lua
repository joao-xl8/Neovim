require('lspsaga').setup({
  -- Basic configuration that should work with most versions
  ui = {
    border = 'rounded',
  },
  lightbulb = {
    enable = true,
    sign = true,
    virtual_text = false,
  },
})
